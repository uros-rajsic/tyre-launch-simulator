# Tyre Launch Simulator

A browser-based 3D tyre launch simulator.

## Run Locally

On Windows, double-click:

```text
start-local.cmd
```

Or run:

```bash
npm run dev
```

Then open:

```text
http://localhost:4174
```
